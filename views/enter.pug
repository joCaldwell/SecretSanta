
doctype html
html
   head
        title enter 
        link(rel="stylesheet", href=css , type="text/css")
   body
     h2 Enter your lobby
     div(id="lobby_list")
          each val in games
               a(href="lobby/"+val, class="lobby_link") #{val}