doctype html
html
   head
      title create
      link(rel="stylesheet", href=css , type="text/css")
   body
      h1#create_title Create A Lobby! 
      br
      br
      form(action="/create", method="post" id="create_form")
         div(class="grid_container")
            label(for="lobby" class="create_label") Lobby Name: 
            input(name="lobby" class="create_input")
            label(for="creator_name" class="create_label") Your Name: 
            input(name="creator_name" class="create_input")
            label(for="creator_code" class="create_label") Your Code: 
            input(name="creator_code" class="create_input")
         br(id="break-after-grid")
         button(type = "submit", id="submit_button") SUBMIT!